const Bee = require('./2-Bee');
// 9) `age` 속성은 `10`이어야 합니다
// 10) `job` 속성은 `make honey`이어야 합니다
// 11) `color` 속성은 `Bee`로부터 상속받습니다
// 12) `food` 속성은 `Grub`으로부터 상속받습니다
// 13) `eat` 메서드는 `Grub`으로부터 상속받습니다
// 14) `honeyPot` 속성은 `0`이어야 합니다
// 15) `makeHoney` 메서드는 `honeyPot`에 1씩 추가합니다
// 16) `giveHoney` 메서드는 `honeyPot`에 1씩 뺍니다
class HoneyMakerBee {
  // TODO..
}

module.exports = HoneyMakerBee;
